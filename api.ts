// 将操作信息存储到 home dir 下的 .todos 文件中

const add = (title: string) => {
  // 提前思考如何使用，然后进行实现
  // const tasks = db.read()
  // tasks.push({ title, done: false})
  // db.write(tasks)
};

const clear = () => {

};

export { add, clear };
